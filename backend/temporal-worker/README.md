# temporal-worker

Placeholder. Filled in Sprint 3: agentic `InsightGenerationWorkflow` and chat memory consolidation (`ChatMemoryConsolidationWorkflow` on `chat-queue`). Same service — not a separate chat worker.

```bash
pip install -e ../shared
```
